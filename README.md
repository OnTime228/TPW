## Scarecrow Stats Bot

### Запуск
1) Создай `.env`:
- скопируй `.env.example` → `.env`
- вставь `BOT_TOKEN`

2) Положи данные:
- `./data/videos.json` (или ZIP с JSON и тогда `DATA_PATH=/data/your.zip`)

3) Запуск:
```bash
docker compose up --build